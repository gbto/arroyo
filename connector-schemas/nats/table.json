{
    "type": "object",
    "title": "NatsTable",
    "properties": {
        "server": {
            "title": "Server",
            "type": "string",
            "description": "The NATS server that should receive the message",
            "examples": [
                "nats://localhost:4222"
            ]
        },
        "subject": {
            "title": "Subject",
            "type": "string",
            "description": "The subject of the NATS message",
            "examples": [
                "my-subject"
            ]
        },
        "user": {
            "title": "User",
            "type": "string",
            "description": "Optional, the user for NATS server authentication",
            "examples": [
                "my-user"
            ]
        },
        "password": {
            "title": "Password",
            "type": "string",
            "description": "Optional, the password for NATS server authentication",
            "examples": [
                "my-password"
            ]
        },
        "token": {
            "title": "Token",
            "type": "string",
            "description": "Optional, the token for NATS server authentication",
            "examples": [
                "my-auth-token"
            ]
        }
    },
    "additionalProperties": false,
    "required": [
        "server",
        "subject"
    ]
}